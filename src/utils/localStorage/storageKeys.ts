/**
 * Keys localstorage
 */
export const STORAGE_KEYS = {
    FILTER_NAME: "filters.active.name",
    FILTER_FOIL_ONLY: "filters.active.foilOnly",
    FILTER_IS_NON_FOIL_ONLY: "filters.active.isNonFoilOnly",
    FILTER_ONLINE_ONLY: "filters.active.onlineOnly",
    FILTER_CARD_COUNT_MAX: "filters.active.cardCountMax",
} as const;
